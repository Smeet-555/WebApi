<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <!--
    Domain has NO project references — it sits at the bottom of the dependency chain.
    All other layers depend on Domain, never the other way around.
  -->

</Project>
